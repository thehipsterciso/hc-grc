# Study Charter — <FRAMEWORK NAME>

> Copy this directory to `frameworks/<kebab-name>/` to add a framework to the program.

## Identity
- **Framework:** <full name + version>
- **Short ID:** <F-id, e.g. F6>
- **Source:** <canonical source / repo / URL>
- **License:** <license; native control text available? yes/no>
- **Role:** corpus (and: crosswalk? sector-specific? OT? — note any special role)

## Licensing status (human tripwire T2)
- [ ] License confirmed permits ingesting and embedding control text.
- [ ] If paywalled: rights cleared, OR study runs on identifiers/metadata only with the text gap
      recorded as a stated limitation. (A legal/cost decision — not an adversary's call.)

## Scope
This study runs the program pipeline (S1f–S9 in `docs/PROGRAM_ROADMAP.md`) on this framework using
the frozen question set (Q1–Q8 in `docs/PREREGISTRATION.md`). It invents no new questions; it
instantiates the registered ones against this framework's structure.

## Framework-specific notes
- Control count / granularity:
- Native taxonomy (categories, families, functions):
- Internal cross-reference structure (for Q6):
- Known peculiarities the adversary should watch for:

## Outputs
Per-control assets + per-question findings written to the registry with full lineage, each carrying
a same-discipline adversary certificate. Robust/provisional labels assigned in S8f.

## Status
Stub — not started. Blocked on P1 (pre-registration freeze) and S3 (method validation).
