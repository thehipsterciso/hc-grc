# Minimal dependencies for CI test runs (Phase 0 tests only).
# Does NOT include the full ML stack (PyTorch, transformers, etc.) — those are
# Phase 1 execution dependencies. CI runs the structural/protocol tests.
#
# Versions are bounded to the same major as requirements.txt so CI exercises the
# versions production runs on (pass-2 #8/#191). The Tier-2/Tier-3 LLM clients
# (langchain-ollama, claude-agent-sdk) are intentionally omitted: the suite runs
# with HCGRC_DISABLE_REASONING=1 and imports them lazily, so they are never needed.
langgraph>=1.2,<2
langgraph-checkpoint-postgres>=3.1,<4
langchain-core>=1.4,<2
# reasoning_client imports the OTel API at module load; config loads YAML.
opentelemetry-sdk>=1.25,<2
PyYAML>=6.0,<7
# Match the production floors (requirements.txt) so CI exercises the same major
# versions that run on the py3.14 node, not stale ones (#264).
numpy>=2.1,<3
scikit-learn>=1.5,<2
pytest>=8.2.2
pytest-cov>=5.0.0
