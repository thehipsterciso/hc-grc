# NOTE: floors, not exact ==, because the exact pins below predated the Python
# 3.14 migration and had no 3.14 wheels (unbuildable on the production node,
# pass-4 #8/#10). Floors match the versions validated live on 3.14; a generated
# lockfile is the next step for exact reproducibility.
#
# Core data science
numpy>=2.1,<3
pandas>=2.2,<3
polars>=0.20
scipy>=1.13,<2
statsmodels>=0.14
pingouin>=0.5.4

# Machine learning
scikit-learn>=1.5,<2
xgboost>=2.0
lightgbm>=4.3

# NLP
transformers>=4.41
sentence-transformers>=3.0
tokenizers>=0.19
spacy>=3.7

# Graph
networkx>=3.3
torch-geometric>=2.5
python-igraph>=0.11

# Topic modeling / clustering
bertopic>=0.16
gensim>=4.3
umap-learn>=0.5
hdbscan>=0.8.38

# Dimensionality reduction / visualization
matplotlib>=3.9
seaborn>=0.13
plotly>=5.22
pyvis>=0.3

# Data validation
pydantic>=2.7,<3

# Config (platform.yaml loader)
PyYAML>=6.0,<7

# Experiment tracking (MLflow 3 — local SQLite backend; file store deprecated)
mlflow>=3.1,<4

# Agent orchestration.
# Bounded to the installed major versions — the prior `>=0.2.28` ranges silently
# resolved to 1.x/3.x, making installs non-reproducible (Hardening pass 1, #13).
langgraph>=1.2,<2
langgraph-checkpoint-postgres>=3.1,<4
langchain-core>=1.4,<2

# LLM execution routing (ADR-0016) — reasoning_client backends.
# Tier 2 (local LLM): langchain-ollama drives the on-device Ollama server.
# Tier 3 (frontier): claude-agent-sdk routes to Claude Max 20x via the
#   `claude` CLI's subscription auth (CLAUDE_CODE_OAUTH_TOKEN — NO Anthropic API key).
langchain-ollama>=1.1,<2
claude-agent-sdk>=0.2.101,<0.3

# Database (PostgresSaver) — requires psycopg 3, NOT psycopg2.
# langgraph-checkpoint-postgres >=2.x builds its saver on psycopg 3.
psycopg[binary,pool]>=3.2

# Observability (local, self-hosted — LangSmith disqualified, SaaS-only).
# Upper-bounded for reproducible installs (#243); a full lockfile is the next step.
arize-phoenix>=7.0,<18
openinference-instrumentation-langchain>=0.1.40,<1
opentelemetry-sdk>=1.25,<2
opentelemetry-exporter-otlp>=1.25,<2

# Vector store (Qdrant) — local, data-sovereign client (used by
# src/infrastructure/vector_store/qdrant_setup.py; was imported but undeclared).
qdrant-client>=1.9,<2

# Provenance (W3C PROV-DM)
rdflib>=7.0.0,<8

# Data versioning (DVC — local remote only, telemetry disabled; no SaaS)
dvc>=3.0,<4

# Jupyter
jupyter==1.0.0
jupyterlab==4.2.2
nbconvert==7.16.4

# Code quality
black==24.4.2
ruff==0.4.8
mypy==1.10.0
pytest==8.2.2
pytest-cov==5.0.0
pytest-asyncio>=0.23.7
